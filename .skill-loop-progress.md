# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/beads_viewer — `bv --robot-triage` and the robot path
# Total Passes: 10 (minimum)
# Started: 2026-06-07

## Status: IN PROGRESS — Pass 2 of 10

## HONEST baselines (warm, ISOLATED cache via XDG_CACHE_HOME, this repo)
The originally-recorded 2.87/0.90/8.40 were contaminated: insights 8.4s was a one-time
COLD exact-betweenness compute; plan 0.90s reflected a 6.6MB GLOBAL cache holding other
projects' entries. True steady state with a healthy per-repo cache:
- triage:   2.295 s   (≈2.1s of this is the correlation git fan-out; plan path is 0.18s)
- plan:     0.176 s
- insights: 0.819 s   (warm/cached; COLD exact compute is ~8.4s — a separate target)
- cold cache-bloat case (6.6MB global cache) remains real-world relevant for codec/shape passes.

## REORDERED missions (front-load biggest levers; breakthroughs first)
2. Correlation-BatchGit (THE triage breakthrough): 336 `git show` -> 1 `git log --name-status --numstat`.
3. Insights-Betweenness: cold 8.4s exact -> approx/parallel/iterative (franken_networkx CSR/Brandes).
4. DiskCache-FastCodec: stdlib encoding/json -> goccy; streaming read (helps cold + bloated cache + insights write).
5. DiskCache-CompactShape: shrink payload (int-indexed arrays vs per-node map[string]float64).
6. Insights-CyclesHitsEig: iterative-convergence + caps; skip-when-dense.
7. XFetch/Correlation-Scope: stop near-constant XFetch recompute on tiny/fast repos; bound correlation.
8. Loader-Allocs: cut 22.8k allocs / 7.7MB on the 1.9MB JSONL parse.
9. DataHash: faster/incremental hashing (avoid full sort+SHA256 each call).
10. GraphBuild-IntIndexed + final: CSR adjacency + int-indexed metric write-back; end-to-end re-bench.

## Missions (ordered by Impact×Confidence/Effort)
1. DiskCache-NoRewriteOnHit: stop rewriting the 6.6MB analysis_cache.json on every read/hit (LRU touch). Affects ALL robot cmds.
2. DiskCache-FastCodec: replace stdlib encoding/json with goccy/go-json for cache (de)serialize; streaming read.
3. DiskCache-CompactShape: shrink cache payload — int-indexed arrays instead of per-node map[string]float64; cap/omit redundant maps.
4. Correlation-BatchGit: replace 336 `git show` (2/commit) with ONE `git log --name-status --numstat <range>`; reuse snapshot stream parser.
5. Correlation-Scope: bound/cache/parallelize remaining correlation work for triage; ensure --robot-next doesn't over-correlate.
6. Insights-Betweenness: size-gate to approximate sooner + parallelize/iterative Brandes (mine franken_networkx CSR/iterative).
7. Insights-CyclesHitsEig: iterative-convergence + caps for cycles/HITS/eigenvector; skip-when-dense; franken_networkx techniques.
8. Loader-Allocs: cut 22.8k allocs / 7.7MB on the 1.9MB JSONL parse (goccy decoder reuse, buffer pooling).
9. DataHash: faster + incremental hashing (avoid full sort+SHA256 each call); xxhash/structural.
10. GraphBuild-IntIndexed + final: CSR adjacency + int-indexed metric write-back (alien-graveyard/alien-artifact ceiling); end-to-end re-bench + artifact update.

## Completed Passes
(none yet)

## Completed Passes

### Pass 1 — DiskCache-NoRewriteOnHit — b5ed373
- Files: pkg/analysis/cache.go (+ cache_test.go benchmark)
- Change: getRobotDiskCachedStats no longer rewrites the whole cache file on a read-hit.
- Bench (warm isolated): insights 819->663ms (-19%); triage 2.295->2.259s; plan flat.
- Verdict: PRODUCTIVE (correct; biggest effect on large cached payloads & bloated caches).

### Pass 2 — Correlation-BatchGit — 974ca5e
- Files: pkg/correlation/cocommit.go
- Change: 336 per-commit `git show` -> 2 batched `git log --no-walk` (name-status+numstat), memoized.
- Bench: git execs 340->6; triage 2.30s->1.21s (1.87x); next 2.25s->1.21s. PRODUCTIVE.

### Pass 3 — Correlation-LifecycleExtraction — d76cd8c
- Files: pkg/correlation/extractor.go, extractor_snapshot.go (+ harness/goldens)
- Change: dropped `git log -p` over issues.jsonl; --raw blob-OID snapshots, deduped cat-file, maphash multiset; gate 48MB->64KB.
- Bench: triage 1.21s->1.025s; next ->1.016s. Differential 1776==1776 events. PRODUCTIVE.

## Status: IN PROGRESS — Pass 4 of 10 (Correlation result cache)

### Pass 4 — Correlation-ResultCache — 936dbca
- Files: pkg/correlation/disk_cache.go (new), file_lock_unix/windows.go (new), cmd/bv/robot_registry.go
- Change: persistent HistoryReport cache keyed HEAD+beads-hash+opts+version (goccy); skips git extraction on hit.
- Bench: repeat triage 1.02s->152ms (6.7x); cold parity; invalidation verified. PRODUCTIVE.

### Pass 5 — Datasource-Validate-Dedup — (this commit)
- Files: internal/datasource/validate.go (+ goldens)
- Change: typed probe instead of map[string]interface{}; path/mtime/size validation cache (validate once not 2-3x).
- Bench: warm triage 152->108ms (-27%); next ->106ms. base==cand identical. PRODUCTIVE.

## Status: IN PROGRESS — Pass 6 of 10 (insights cold: exact betweenness)

## Note (post-pass-5 re-measurement)
- The originally-reported "insights 8.4s" was a CONTAMINATED-CACHE ARTIFACT, not a real hotspot.
  Insights cold is now 0.51-0.75s with ALL metrics computed (approx betweenness sample=100, ~1.6ms;
  every metric sub-2ms). Dropped the "insights betweenness/cycles breakthrough" missions as phantom.
- Real remaining levers (perf-attributed on warm plan/triage floor ~110ms):
  (a) analysis disk cache READ still uses stdlib encoding/json (~6%; ~0.9s on a bloated 6.6MB cross-project cache).
  (b) JSONL parsed TWICE (datasource validate probe + loader parse) over the same 1.9MB.
  (c) cold triage 1.05s = correlation extraction (232MB blob I/O) on first call after a change.
  (d) Go binary startup/init overhead (~70ms of the 110ms; heavy deps).

### Pass 6 — AnalysisDiskCache-FastCodec — 7a457b8
- File: pkg/analysis/cache.go. stdlib encoding/json -> goccy streaming+compact.
- Bench: bloated 13MB cache read 2.29x (325->142ms); small plan 110->98ms, triage 123->113ms. PRODUCTIVE.

### Pass 7 — AnalysisCache-CompactShape — (this commit)
- Files: pkg/analysis/cache.go (+ cache_extra_test.go, cache_test.go). Columnar SoA blob, version 1->2.
- Bench: cache -44%/-58%, decode 1.61x; insights 489->377ms, triage ->116ms. ubs 0/0. PRODUCTIVE.

## Status: IN PROGRESS — Pass 8 of 10 (parse JSONL once: loader/validate dedup)

### Pass 8 — Datasource-ParseOnce — 11bc515
- Files: internal/datasource/load.go, select.go, pkg/loader/loader.go, cmd/bv/main.go
- Change: loader's tolerant parse doubles as validation (ParseStats hook); robot path parses issues.jsonl once.
- Bench: opens 2->1; plan 108->95ms, triage 121->114ms. Full suite+e2e green. PRODUCTIVE.

## Status: IN PROGRESS — Pass 9 of 10

### Pass 9 — ComputeDataHash-Dedup — (committed)
- Files: pkg/analysis/{graph,cache,triage}.go, cmd/bv/{main,robot_registry}.go
- Change: memoize data hash (sync.Once) + thread once through dispatch; data_hash unchanged.
- Bench: hash calls 2->1; plan 89->85ms, triage 105->102ms, insights 395->368ms. PRODUCTIVE.

## Status: IN PROGRESS — Pass 10 of 10 (parallel JSONL load + final wrap)

### Pass 10 — Loader-ParallelParse — ff1bc91
- Files: pkg/loader/loader.go (+ parallel_diff_test.go, benchmark_test.go, HOTSPOT_TABLE.md)
- Change: size-gated (>=4MiB) morsel-driven parallel JSONL parse; serial below crossover (GC-bound). -race + differential proven.
- Bench: net-neutral on 1.9MB repo (no regression), +10-21% on multi-MB stores. PRODUCTIVE (future-proof).

## Status: COMPLETE — 10/10 passes done. Full suite green (e2e incl). 0 failures.
## FINAL (orig 0ef0e25 vs final, isolated cache, median-of-3):
##   triage  warm 2.24s -> 0.09s (~25x), cold 2.20s -> 0.98s
##   next    warm 2.22s -> 0.08s (~28x), cold 2.20s -> 0.98s
##   plan    warm 0.14s -> 0.06s (~2.3x)
##   insights warm 0.63s -> 0.19s (~3.3x); git execs(triage) 340 -> 3

## EXTENSION — cold first-call (user-approved: safe wins + incremental extraction)
Cold profile (0.98s): correlation extractViaSnapshots ~31% (readBlobs/readFull 17%, parseDiff/parseBeadJSON 6% stdlib-json) + git inflate/pipe of 232MB (~200 historical issues.jsonl snapshots).
11. Correlation-CacheSplit: extraction is HEAD-only but report cache keys on beads-hash too -> br-edit busts the 232MB extraction. Memoize []BeadEvent keyed on HEAD(+opts) alone; reassemble report per beads-hash cheaply.
12. Correlation-ParseBeadGoccy: parseBeadJSON stdlib encoding/json -> goccy + drop checkValid.
13. Correlation-IncrementalExtract: content-addressed per-commit/blob-OID-pair memo so HEAD-advance only processes NEW commits (alien-graveyard content-addressed memoization).

## Status: IN PROGRESS — Pass 11 (Correlation-CacheSplit)

### Pass 11 — Correlation-CacheSplit — d02f5cb  | edit->triage 0.98s->0.15s
### Pass 12 — Correlation-ParseBeadGoccy — f2383c5 | parseBeadJSON goccy
### Pass 13 — Correlation-IncrementalExtract — 609912f | new-commit->triage 0.99s->0.40s; 0 blobs when warm

## Status: COMPLETE — 13 passes. Cold-path scenario matrix (orig 0ef0e25 -> final):
##   warm-repeat 2.26s->0.10s | edit->triage ->0.15s | new-commit->triage ->0.40s | cold-first-ever ->1.01s
## Next candidate (not done): co-commit primeBatch -> per-commit SHA cache (residual on new-commit path).

## Pass 14 — Correlation-CoCommitIncremental (TODO tracker)
- [x] P14.1 investigate cocommit.go (primeBatch memoizes fileCache/statCache per SHA; pure fn of SHA+excludePathspecs)
- [x] P14.2 design per-commit co-commit cache (SHA key, namespaced by exclude-pathspec hash, goccy, flock, bounds)
- [x] P14.3 implement per_commit_cocommit_cache.go + wire primeBatch (disk-load known, git only unknown, persist fresh)
- [x] P14.4 differential test (full/cold/warm/k-new byte-identical; fetch count == uncached count)
- [x] P14.5 verify: goldens cold+warm, -race, correlation+cmd/bv tests; handle date-drift goldens (regen if byte-identical to HEAD)
- [x] P14.6 gates: build/vet/gofmt/ubs (confirm ubs criticals are the established benign patterns)
- [x] P14.7 benchmark new-commit path (co-commit git calls 2->0 confirmed; warm/edit/cold unchanged)
- [x] P14.8 commit pass 14 (agent committed 7e8a6bd+5c483de; add golden regen if needed)
- [x] P14.9 final comprehensive re-benchmark (orig vs final all scenarios) + artifact/memory update + cleanup

## Fresh-eyes bug review (4 parallel adversarial review agents over the 19 changed files)
Found + fixed (all output-preserving; candidate byte-identical to pre-fix HEAD on all 4 robot cmds):
- BUG-A1 (SERIOUS): head_artifact_cache dropped CorrelatedCommit.BeadID (json:"-") -> middle-tier
  (edit-bead->triage) produced commit-less reports. Fix: custom historyArtifact (Un)MarshalJSON
  preserving commit BeadIDs via parallel array. Regression tests added. E2E: total_commits 275==275.
- BUG-D1 (SERIOUS): loadSmart accepted a fresher non-issue/empty-content JSONL (sprints.jsonl) as
  0-issue "success", shadowing real issues.jsonl. Fix: ParseStats.Skipped counter + reject candidate
  with 0 valid issues from non-empty content; empty-project still valid. Regression tests added.
- (caught by pass-10 differential test) parallel parser didn't merge ParseStats.Skipped -> fixed merge.
- BUG-B1: transient git failure in primeBatch persisted empty co-commit entries (poison 30d). Fix:
  batchFilesChanged/batchLineStats return error; skip disk persist on failure (in-memory degrades for run only).
- RISK-B2: readBlobs could deadlock on a cat-file parse error mid-stream (full pipe). Fix: Kill git on
  parse error before draining writeErr (mirrors legacy -p path).
- BUG-D2: parallel parser used > maxCapacity vs serial's >= (bufio isPrefix) -> exactly-maxCapacity line
  diverged. Fix: >=.
- BUG-D3: defaultProbe treated present-but-null required field as absent (vs old map semantics + rawProbe).
  Fix: non-pointer json.RawMessage values, presence = len>0.
- SMELL-D5: corrected inaccurate json.Decoder comment.
Reviewed-correct (no bug): analysis SoA cache round-trip, version gating, DataHash memoization/seeding,
no-rewrite-on-hit, goccy codec, incremental-extract order composition, blob-OID validation.
Not changed (assessed low/narrow): SMELL-B3 dup-id winner (one-record-per-id invariant), RISK-D4
validation-cache mtime granularity in watch sessions (nanosecond mtime makes it ~unreachable).
